
%这是c_2,c_1把1/10去掉就好了

m=616;
A = (1/10)*[m, m-4; 6-m, 10-m];
x0 = [1; 2];
[P, D] = eig(A);
a = P \ x0;
syms n;
xn = P * (D^n) * a;
disp('通项 x(n) 为:');
disp(xn);




%ai给的
% % 定义矩阵 A
% m = 616; % 假设 m 的值，可以根据实际情况调整
% A = [m, m-4; 6-m, 10-m];
% 
% % 定义初始向量 x0
% x0 = [1; 2];N = 10; % 计算前 10 个向量
% x = zeros(2, N); % 初始化存储向量的矩阵
% x(:, 1) = x0; % 第一个向量是初始向量
% 
% % 迭代计算向量序列
% for n = 2:N
%     x(:, n) = A * x(:, n-1);
% end
% 
% % 显示结果
% disp(x);
% syms  n;
% A = [m, m-4; 6-m, 10-m];
% x0 = [1; 2];
% 
% % 计算 A 的特征值和特征向量
% [V, D] = eig(A);
% 
% % 计算 A^n
% An = V * D^n * inv(V);
% 
% % 计算 x(n)
% xn = An * x0;
% 
% % 显示通项公式
% disp(xn);